<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="330dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send credit"
        android:textSize="20sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="10dp"
        android:id="@+id/send_credit_tv"
        android:textColor="@color/colorPrimaryDark"/>

    <EditText
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter the ammount you want to send"
        app:layout_constraintTop_toBottomOf="@id/send_credit_tv"
        app:layout_constraintStart_toStartOf="@id/send_credit_tv"
        android:layout_marginTop="25dp"
        android:id="@+id/ammount_et"
        android:inputType="number"/>

    <Button
        android:id="@+id/send_credit_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send"
        app:layout_constraintTop_toBottomOf="@id/ammount_et"
        app:layout_constraintEnd_toEndOf="@id/ammount_et"
        android:background="@color/colorAccent"
        android:layout_marginTop="15dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="15dp"/>

</android.support.constraint.ConstraintLayout>